"""設定管理モジュール

ScrapingConfigデータクラスおよびライブラリ内で使用する定数を定義する。
"""

import os
from dataclasses import dataclass, field


@dataclass
class ScrapingConfig:
    """スクレイピングの設定を保持するデータクラス

    Attributes:
        netkeiba_base_url (str): netkeibaのベースURL
        netkeiba_race_url (str): netkeibaのレースURL
        jra_url (str): JRAのURL
        headers (dict[str, str]): HTTPリクエストヘッダー
        chrome_driver_path (str | None): ChromeDriverのパス
            環境変数 ``CHROME_DRIVER_PATH`` が設定されていればその値を使用し、
            未設定の場合は ``None`` (SeleniumのService自動検出) をデフォルトとする。
        request_timeout (int): HTTPリクエストのタイムアウト(秒)
    """

    netkeiba_base_url: str = "https://db.netkeiba.com"
    netkeiba_race_url: str = "https://race.netkeiba.com"
    jra_url: str = "https://www.jra.go.jp"
    headers: dict[str, str] = field(
        default_factory=lambda: {
            "User-Agent": (
                "Mozilla/5.0 (Windows NT 10.0; Win64; x64) "
                "AppleWebKit/537.36 (KHTML, like Gecko) "
                "Chrome/58.0.3029.110 Safari/537.3"
            )
        }
    )
    chrome_driver_path: str | None = field(
        default_factory=lambda: os.environ.get("CHROME_DRIVER_PATH")
    )
    request_timeout: int = 10


# ---------------------------------------------------------------------------
# 競馬場辞書
# ---------------------------------------------------------------------------
KEIBAJO_TO_ID_DICT: dict[str, str] = {
    "札幌": "01",
    "函館": "02",
    "福島": "03",
    "新潟": "04",
    "東京": "05",
    "中山": "06",
    "中京": "07",
    "京都": "08",
    "阪神": "09",
    "小倉": "10",
    "門別": "30",
    "盛岡": "35",
    "水沢": "36",
    "浦和": "42",
    "船橋": "43",
    "大井": "44",
    "川崎": "45",
    "金沢": "46",
    "笠松": "47",
    "名古屋": "48",
    "園田": "50",
    "姫路": "51",
    "高知": "54",
    "佐賀": "55",
    "帯広": "65",
}

ID_TO_KEIBAJO_DICT: dict[str, str] = {v: k for k, v in KEIBAJO_TO_ID_DICT.items()}

# ---------------------------------------------------------------------------
# グレード辞書
# ---------------------------------------------------------------------------
GRADE_DICT: dict[str, str] = {
    "1": "G1",
    "2": "G2",
    "3": "G3",
    "4": "重賞",
    "15": "L",
    "5": "OP",
    "10": "JG1",
    "11": "JG2",
    "12": "JG3",
    "19": "Jpn1",
    "20": "Jpn2",
    "21": "Jpn3",
}

# ---------------------------------------------------------------------------
# 重量種別
# ---------------------------------------------------------------------------
WEIGHT_CONDITIONS: list[str] = ["定量", "別定", "ハンデ", "馬齢"]

# ---------------------------------------------------------------------------
# 払い戻し表のカラム名
# ---------------------------------------------------------------------------
PAYBACK_COLUMNS: list[str] = ["券種", "馬番", "払戻", "人気"]

# ---------------------------------------------------------------------------
# 馬券の名称リスト
# ---------------------------------------------------------------------------
TWO_COMBINATION_BETS: list[str] = ["枠連", "馬連", "ワイド", "馬単"]
THREE_COMBINATION_BETS: list[str] = ["3連複", "3連単"]

# ---------------------------------------------------------------------------
# レース情報
# ---------------------------------------------------------------------------

# レース情報のカラム
RACE_INFO_COLUMNS: list[str] = [
    "レースID",
    "日付",
    "曜日",
    "レース名",
    "発走時刻",
    "天候",
    "馬場",
    "レース種別",
    "芝ダ",
    "距離",
    "左右",
    "コース",
    "内外",
    "競馬場",
    "回",
    "開催日",
    "競走種別",
    "競走条件",
    "グレード",
    "競走記号",
    "重量種別",
    "頭数",
    "1着賞金",
    "2着賞金",
    "3着賞金",
    "4着賞金",
    "5着賞金",
]

# ---------------------------------------------------------------------------
# 出馬表
# ---------------------------------------------------------------------------

# 出馬表のカラム
ENTRY_COLUMNS: list[str] = [
    "レースID",
    "出走区分",
    "枠",
    "馬番",
    "馬名",
    "性別",
    "年齢",
    "斤量",
    "騎手",
    "所属",
    "厩舎",
    "馬体重",
    "増減",
    "馬ID",
    "騎手ID",
    "厩舎ID",
]

# 出馬表のNaN不可カラム
# 枠・馬番は出走確定前（想定段階）ではNaNになる
# 騎手IDは騎手未確定（"○○"）の場合NaNになる
ENTRY_NON_NAN_COLUMNS: list[str] = [
    "レースID",
    "出走区分",
    "馬名",
    "性別",
    "年齢",
    "斤量",
    "騎手",
    "所属",
    "厩舎",
    "馬ID",
    "厩舎ID",
]

# 出馬表の出走区分の有効値（結果と異なり"中止"は含まれない）
VALID_ENTRY_STATUSES: set[str] = {"出走", "取消", "除外"}

# 出馬表の仮カラム（スクレイピング直後のもの）
SHUTUBA_RAW_COLUMNS: list[str] = [
    "枠",
    "馬番",
    "削除",
    "馬名",
    "性齢",
    "斤量",
    "騎手",
    "厩舎",
    "馬体重(増減)",
    "オッズ",
    "人気",
    "削除",
    "削除",
    "削除",
]

# ---------------------------------------------------------------------------
# レース結果
# ---------------------------------------------------------------------------

# レース結果のカラム
RESULT_COLUMNS: list[str] = [
    "レースID",
    "異常区分",
    "着順",
    "枠",
    "馬番",
    "馬名",
    "性別",
    "年齢",
    "斤量",
    "騎手",
    "タイム",
    "着差",
    "人気",
    "単勝オッズ",
    "後3F",
    "1コーナー通過順",
    "2コーナー通過順",
    "3コーナー通過順",
    "4コーナー通過順",
    "所属",
    "厩舎",
    "馬体重",
    "増減",
    "馬ID",
    "騎手ID",
    "厩舎ID",
]

# 異常区分の有効値
VALID_IJO_KUBUN: set[str] = {"", "取消", "除外", "中止", "失格", "降着"}

# NaN不可カラム（全行で値が必須）
NON_NAN_COLUMNS: list[str] = [
    "レースID",
    "異常区分",
    "枠",
    "馬番",
    "馬名",
    "性別",
    "年齢",
    "斤量",
    "騎手",
    "所属",
    "厩舎",
    "馬ID",
    "騎手ID",
    "厩舎ID",
]

# 列挙型バリデーション
VALID_GENDERS: set[str] = {"牡", "牝", "セ"}
VALID_AFFILIATIONS: set[str] = {"美浦", "栗東", "地方", "海外"}

# ---------------------------------------------------------------------------
# コーナー通過順
# ---------------------------------------------------------------------------

# コーナー通過順のカラム
CORNER_COLUMNS: list[str] = [
    "レースID",
    "1コーナー通過順",
    "2コーナー通過順",
    "3コーナー通過順",
    "4コーナー通過順",
]

# ---------------------------------------------------------------------------
# 単勝払い戻し（同着は3頭まで考慮）
# ---------------------------------------------------------------------------
WIN_PAYOFF_COLUMNS: list[str] = ["レースID"] + [
    col for i in range(1, 4) for col in [f"単勝払戻金_{i}", f"単勝馬番_{i}", f"単勝人気_{i}"]
]

# ---------------------------------------------------------------------------
# 複勝払い戻し（同着は3頭まで考慮）
# ---------------------------------------------------------------------------
SHOW_PAYOFF_COLUMNS: list[str] = ["レースID"] + [
    col for i in range(1, 6) for col in [f"複勝払戻金_{i}", f"複勝馬番_{i}", f"複勝人気_{i}"]
]

# ---------------------------------------------------------------------------
# 枠連払い戻し（同着は3頭まで考慮）
# ---------------------------------------------------------------------------
BRACKET_PAYOFF_COLUMNS: list[str] = ["レースID"] + [
    col
    for i in range(1, 4)
    for col in [
        f"枠連払戻金_{i}",
        f"枠連組番_{i}_1",
        f"枠連組番_{i}_2",
        f"枠連人気_{i}",
    ]
]

# ---------------------------------------------------------------------------
# 馬連払い戻し（同着は3頭まで考慮）
# ---------------------------------------------------------------------------
QUINELLA_PAYOFF_COLUMNS: list[str] = ["レースID"] + [
    col
    for i in range(1, 4)
    for col in [
        f"馬連払戻金_{i}",
        f"馬連組番_{i}_1",
        f"馬連組番_{i}_2",
        f"馬連人気_{i}",
    ]
]

# ---------------------------------------------------------------------------
# ワイド払い戻し（同着は3頭まで考慮）
# ---------------------------------------------------------------------------
QUINELLA_PLACE_PAYOFF_COLUMNS: list[str] = ["レースID"] + [
    col
    for i in range(1, 8)
    for col in [
        f"ワイド払戻金_{i}",
        f"ワイド組番_{i}_1",
        f"ワイド組番_{i}_2",
        f"ワイド人気_{i}",
    ]
]

# ---------------------------------------------------------------------------
# 馬単払い戻し（同着は3頭まで考慮）
# ---------------------------------------------------------------------------
EXACTA_PAYOFF_COLUMNS: list[str] = ["レースID"] + [
    col
    for i in range(1, 7)
    for col in [
        f"馬単払戻金_{i}",
        f"馬単組番_{i}_1",
        f"馬単組番_{i}_2",
        f"馬単人気_{i}",
    ]
]

# ---------------------------------------------------------------------------
# 3連複払い戻し（同着は3頭まで考慮）
# ---------------------------------------------------------------------------
TRIO_PAYOFF_COLUMNS: list[str] = ["レースID"] + [
    col
    for i in range(1, 4)
    for col in [
        f"3連複払戻金_{i}",
        f"3連複組番_{i}_1",
        f"3連複組番_{i}_2",
        f"3連複組番_{i}_3",
        f"3連複人気_{i}",
    ]
]

# ---------------------------------------------------------------------------
# 3連単払い戻し（同着は3頭まで考慮）
# ---------------------------------------------------------------------------
TRIFECTA_PAYOFF_COLUMNS: list[str] = ["レースID"] + [
    col
    for i in range(1, 7)
    for col in [
        f"3連単払戻金_{i}",
        f"3連単組番_{i}_1",
        f"3連単組番_{i}_2",
        f"3連単組番_{i}_3",
        f"3連単人気_{i}",
    ]
]

# ---------------------------------------------------------------------------
# ラップタイム（100mごと、最大5000mまで）
# ---------------------------------------------------------------------------
LAP_TIME_COLUMNS: list[str] = ["レースID", "ペース"] + [f"{i}m" for i in range(100, 5001, 100)]

# ---------------------------------------------------------------------------
# オッズ
# ---------------------------------------------------------------------------
ODDS_COLUMNS: list[str] = [
    "馬番",
    "単勝オッズ",
    "単勝人気",
    "複勝最小オッズ",
    "複勝最大オッズ",
    "複勝人気",
]

# netkeiba予想オッズ（馬券発売前）
YOSO_ODDS_COLUMNS: list[str] = [
    "馬番",
    "馬名",
    "予想単勝オッズ",
]

# ---------------------------------------------------------------------------
# 馬柱
# ---------------------------------------------------------------------------
PAST_PERFORMANCES_COLUMNS: list[str] = [
    "レースID",
    "日付",
    "競馬場",
    "回",
    "開催日",
    "R",
    "レース名",
    "天候",
    "頭数",
    "枠",
    "馬番",
    "単勝オッズ",
    "人気",
    "着順",
    "異常区分",
    "騎手",
    "騎手ID",
    "斤量",
    "芝ダ",
    "距離",
    "馬場",
    "タイム",
    "着差",
    "1コーナー通過順",
    "2コーナー通過順",
    "3コーナー通過順",
    "4コーナー通過順",
    "レース前3F",
    "レース後3F",
    "後3F",
    "馬体重",
    "増減",
    "勝ち馬(2着馬)",
    "賞金",
    "主催",
    "間隔日数",
]

# ---------------------------------------------------------------------------
# 馬情報
# ---------------------------------------------------------------------------
HORSE_INFO_COLUMNS: list[str] = [
    "馬ID",
    "馬名",
    "性別",
    "生年",
    "所属",
    "厩舎",
    "厩舎ID",
    "父",
    "母",
    "母父",
    "馬主",
    "馬主ID",
    "生産者",
    "生産者ID",
    "総賞金(万円)",
]

# 所属の略称→正式名称変換辞書
AFFILIATION_MAP: dict[str, str] = {
    "東": "美浦",
    "西": "栗東",
    "地": "地方",
    "外": "海外",
}

# ---------------------------------------------------------------------------
# レース一覧
# ---------------------------------------------------------------------------
RACE_LIST_COLUMNS: list[str] = [
    "レースID",
    "日付",
    "競馬場",
    "回",
    "開催日",
    "天候",
    "R",
    "レース名",
    "芝ダ",
    "距離",
    "頭数",
    "馬場",
    "タイム",
    "レース前3F",
    "レース後3F",
    "勝ち馬",
    "勝ち馬ID",
    "騎手",
    "騎手ID",
    "所属",
    "厩舎",
    "厩舎ID",
    "2着馬",
    "2着馬ID",
    "3着馬",
    "3着馬ID",
]

# ---------------------------------------------------------------------------
# レーススケジュール
# ---------------------------------------------------------------------------
RACE_SCHEDULE_COLUMNS: list[str] = [
    "レースID",
    "日付",
    "競馬場",
    "回",
    "開催日",
    "R",
    "レース名",
    "芝ダ",
    "距離",
    "頭数",
    "馬場",
    "発走時刻",
]

# ---------------------------------------------------------------------------
# JRA重賞レース一覧
# ---------------------------------------------------------------------------
JRA_GRADED_RACE_COLUMNS: list[str] = [
    "日付",
    "グレード",
    "レース名",
    "競馬場",
    "性齢",
    "芝ダ",
    "距離",
    "優勝馬",
    "騎手",
]
